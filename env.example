# AP2 Vacation Booking Demo - Environment Variables
# Copy this file to .env.local and fill in your actual values

# ==============================================================================
# OpenRouter API Key
# ==============================================================================
# Get your free API key from: https://openrouter.ai/keys
# This is used for the AI agent (LangChain with Grok model)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ==============================================================================
# Stripe API Keys (Test Mode)
# ==============================================================================
# Get your test keys from: https://dashboard.stripe.com/test/apikeys
# Never use production keys in development!

# Publishable key (starts with pk_test_) - safe to expose in browser
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Secret key (starts with sk_test_) - KEEP THIS SECRET, server-side only
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# ==============================================================================
# JWT Secret (Optional - for demo purposes)
# ==============================================================================
# Generate a random string (minimum 32 characters recommended)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_min_32_chars

# ==============================================================================
# Setup Instructions
# ==============================================================================
# 1. Copy this file: cp env.example .env.local (or rename to .env.local)
# 2. Get OpenRouter API key: https://openrouter.ai/keys (free tier available)
# 3. Get Stripe test keys: https://dashboard.stripe.com/test/apikeys (free)
# 4. Generate JWT secret: openssl rand -base64 32
# 5. Paste the actual values in .env.local (replace the placeholders)
# 6. Never commit .env.local to git (it's in .gitignore)
#
# For more details, see SETUP.md

